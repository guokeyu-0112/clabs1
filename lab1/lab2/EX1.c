#include <stdio.h>

int main()
{
    unsighed int a=1,b=0;
    while a>0
    {
        a++;
    }
    printf("the maximum number of unsighed in is:%u\n",a-1)'
     unsighed int n=a-1;
     do
     {
         n=n/10;
         b++;
     }while(n>0)
     printf(there are %d digits in the maximum number of unsighed int,\n",b)
     
     return 0
}